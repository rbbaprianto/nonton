app = "nonton"
primary_region = "sin"

[build]
  dockerfile = "docker-compose.yml"
  builder = "dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
